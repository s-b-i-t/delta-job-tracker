# PR #6 Blocker Closure Matrix

Purpose: provide a deterministic closeout matrix for blockers raised in [docs/teamc_pr6_review.md](/home/bhatt/programming/delta-job-tracker/docs/teamc_pr6_review.md), so reviewers can mark closure quickly when Team C publishes fixes.

Scope: blockers A-D from Team C review section "Failure modes and reporting correctness".

Status template rule:
- Use only `OPEN`, `PARTIAL`, or `CLOSED` in the final status column.
- A blocker is `CLOSED` only when all required evidence in the row is present.

| Blocker ID | Blocker Summary | Intended Correct Behavior | Code Locations Expected to Change | Tests Expected to Prove Closure | Evidence Required to Mark `CLOSED` | Status |
|---|---|---|---|---|---|---|
| A | Host cutoff can be reported as `FAILED` (not `SKIPPED`) | Any `discovery_host_failure_cutoff` path must produce a skip outcome and persist as `SKIPPED` with host-cooldown reasoning, not a hard failure. | `backend/src/main/java/com/delta/jobtracker/crawl/service/CareersDiscoveryService.java` (vendor-probe outcome shaping); `backend/src/main/java/com/delta/jobtracker/crawl/service/CareersDiscoveryRunService.java` (status mapping/persistence). | `CareersDiscoveryServiceTest`: host-cutoff outcome flagged as skipped; `CareersDiscoveryRunServiceTest`: company result for host cutoff persisted as `SKIPPED`, and failed-count not incremented for that case. | 1) Diff showing explicit skip semantics for host-cutoff outcome path. 2) Passing test output for both tests above. 3) Sample run-status/company-result payload snippet showing `SKIPPED` + host cooldown reason. | OPEN |
| B | `hostFailureCutoffSkips` undercounted | Skip counter must include both pre-check skips and host-cutoff events triggered inside vendor probing/discovery flow. | `backend/src/main/java/com/delta/jobtracker/crawl/service/CareersDiscoveryRunService.java` (counter increment logic for mapped host-cutoff outcomes); possibly `backend/src/main/java/com/delta/jobtracker/crawl/service/CareersDiscoveryService.java` (if emitting dedicated signal). | `CareersDiscoveryRunServiceTest`: verifies `hostFailureCutoffSkips` increments for pre-check and vendor-probe host-cutoff path; `CrawlJdbcRepository` persistence test verifies field round-trips in run status. | 1) Diff showing increment path for in-discovery host-cutoff events. 2) Passing tests proving both paths counted. 3) Persisted run-status evidence with expected `hostFailureCutoffSkips` value. | OPEN |
| C | Request budget not strict mid-company | Run-level budget must stop execution immediately when exceeded, including during a single company flow (not only between-company boundaries). Abort reason must be `request_budget_exceeded`. | `backend/src/main/java/com/delta/jobtracker/crawl/service/CareersDiscoveryRunService.java`; likely `backend/src/main/java/com/delta/jobtracker/crawl/http/CanaryHttpBudget.java` and budget-context wiring to enforce hard cap intra-flow. | `CareersDiscoveryRunServiceTest`: mid-company over-budget case aborts with `request_budget_exceeded`; HTTP-budget/context unit test: over-budget request throws/aborts deterministically; optional integration test showing no additional processing after cap breach. | 1) Diff showing hard budget enforcement in-request path (context/hook), not only loop boundary checks. 2) Passing tests for mid-company breach behavior. 3) Run-status evidence with `status=ABORTED`, `stopReason=request_budget_exceeded`, and no post-breach progress growth. | OPEN |
| D | Request counting misses network paths (notably sitemap fetches) | `request_count_total` must include all relevant network fetches used by discovery logic, including sitemap fetch operations. | `backend/src/main/java/com/delta/jobtracker/crawl/sitemap/SitemapService.java`; `backend/src/main/java/com/delta/jobtracker/crawl/service/CareersDiscoveryService.java`; possibly shared metrics/budget hook surfaces. | `SitemapService`/`CareersDiscoveryService` test verifying request counter increments for sitemap fetches; run-service test asserting budget behavior reflects sitemap request volume. | 1) Diff showing explicit request-count hook on sitemap fetch path. 2) Passing tests proving counter increments from sitemap requests. 3) Artifact/log evidence where sitemap-heavy run produces corresponding non-zero request-count growth. | OPEN |

## Reviewer Notes
- If a row has code/test changes but no runtime/status evidence snippet, mark `PARTIAL`.
- If any blocker row remains `OPEN` or `PARTIAL`, PR #6 remains `REQUEST CHANGES`.
